package Collections.Intro;

public class CollectionsDocs {
/*
 * ----------- Collection framework--------------------
 * # Collection is an interface in java.It is foundation for other interfaces like list,map,set.
 * # 
 *   1. List(interface) -> dynamic array
 *       a.ArrayList(class) -> used for storage slower processing
 *       b.LinkedList(class) -> used for processing(faster processing).
 *   2. Set(Interface) -> contains unique element
 *       a.HashSet -> provides data in random order
 *       b.LinkedHashSet -> provides data in the order in which it is stored.
 *       c.TreeSet -> provides data in sorted order.
 *   3.Map(Interface) -> store data in <k,v> pairs.
 *       a.HashMap ->  provides data in random order
 *       b.LinkedHashMap -> provides data in the order in which it is stored.
 *       c.TreeMap ->  provides data in sorted order.
 * 
 */
}
