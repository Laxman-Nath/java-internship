package Arrays;

public class ArrayDocs {
	/*
	 * ---------------------------------Array-------------------------
	 * 	# collection of homogeneous data.
		# collection/group of multiple values of same datatype with fixed size.
		# data stored on the basis of indices(index starts from 0).  
		# also contains duplicate data.
		# random access.
		# contigiuous memory allocation. 
		# array is an object in java
		
		# syntax:
		       
		       data_type array_name[]=new data_type[size];
		       
		            OR
		            new data_type[size]
		  e.g.
		      
		      float height[] = new float[500];
		      char[] alphas = new char[25];
	 */

	public static void main(String[] args) {
	

	}

}
